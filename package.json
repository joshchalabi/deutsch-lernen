{
  "name": "deutsch-lernen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Kanıta dayalı Almanca öğrenme platformu — A1'den C1'e, dört dilli",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "npx esbuild src/lib/fsrs.ts --format=esm --outfile=.tmp-test/fsrs.mjs --log-level=error && node test/fsrs.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
